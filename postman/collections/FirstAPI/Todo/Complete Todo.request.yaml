$kind: http-request
name: Complete Todo
method: PATCH
url: '{{baseUrl}}/api/todo/{{todoId}}?IsComplete=true'
order: 3000
headers:
  - key: Accept
    value: application/json
  - key: Authorization
    value: 'Bearer {{token}}'
description: Mark a todo as complete or incomplete by its ID.
