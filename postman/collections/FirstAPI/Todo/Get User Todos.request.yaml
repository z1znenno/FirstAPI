$kind: http-request
name: Get User Todos
method: GET
url: "{{baseUrl}}/api/todo/"
order: 1000
headers:
  Accept: application/json
  Authorization: Bearer {{token}}
description: Get all todos for a specific user by their ID.
